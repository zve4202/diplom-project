const { dataHistory } = require("./defaultData")
const deliveryPlaces = { lastPlace: "", dataHistory }

module.exports = function () {
        return deliveryPlaces;
}
// }
// module.exports = function (schema, options) {
//     schema.eachPath(function (pathname, schemaType)  {
//         if (schemaType.instance == "Object" && schemaType.options && schemaType.options.getPlaces) {
//             schema.pre('save', function (next) {
//                 if (this.isModified(pathname)) {
//                     schema[pathname] = deliveryPlaces
//                 }
//                 next()
//             })        
//             return;
//         }
//     }   
    
// }
