import React from "react";
// import PropTypes from 'prop-types';

const OrderCard = (props) => {
    const { doc, items } = props;

    return <div></div>;
};

// OrderCard.propTypes = {

// };

export default OrderCard;
